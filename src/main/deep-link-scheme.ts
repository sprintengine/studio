// The URL scheme the app answers to, registered with the OS in `app-lifecycle`.
export const CURRENT_DEEP_LINK_SCHEME = 'sprintengine' as const

export const DEEP_LINK_SCHEMES = [CURRENT_DEEP_LINK_SCHEME] as const
