# A note with no frontmatter

Some files in a real backlog folder were written by hand and never gained a
frontmatter block. Reading one must return the whole file as the body, and a
clear-only write must not invent an empty block above it.
