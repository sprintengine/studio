{
  "id": "claude-code",
  "displayName": "Claude Code",
  "publisher": "anthropic",
  "version": 1,
  "summary": "Anthropic's Claude Code coding agent, driven in a terminal.",
  "category": "Agent CLI",
  "icon": "icon.svg",
  "binary": "claude",
  "permissionPresets": {
    "manual": {
      "label": "Manual — ask before every action",
      "args": ["--permission-mode", "default"]
    },
    "auto": {
      "$comment": "Claude Code's auto mode: a second model (the classifier) reviews each action before it runs instead of the user. NOT workspace-scoped \u2014 the preset this replaced was labelled \"Auto-approve workspace edits\", which described Codex's sandbox rung and misdescribed this one. Reads and in-project edits skip the classifier; shell, network, out-of-project writes and subagent spawns are reviewed.",
      "label": "Auto — the classifier reviews each action",
      "args": ["--permission-mode", "auto"]
    },
    "bypass": {
      "label": "Bypass all approvals (dangerous)",
      "args": ["--permission-mode", "bypassPermissions"]
    }
  },
  "modelSelection": {
    "args": ["--model", "{{model}}"],
    "options": [
      { "id": "claude-fable-5-1", "label": "Fable 5.1" },
      { "id": "claude-opus-5-5", "label": "Opus 5.5" },
      { "id": "claude-opus-5-5[1m]", "label": "Opus 5.5 (1M context)" },
      { "id": "fable", "label": "Fable (latest)" },
      { "id": "claude-opus-5", "label": "Opus 5" },
      { "id": "claude-opus-5[1m]", "label": "Opus 5 (1M context)" },
      { "id": "opus[1m]", "label": "Opus (latest, 1M context)" },
      { "id": "claude-fable-5", "label": "Fable 5" }
    ],
    "allowCustomId": true
  },
  "reasoningSelection": {
    "$comment": "claude --effort <level>, documented in --help on CLI 2.1.220. No default is declared: the CLI's own default effort is undocumented, so declaring one would have to be a guess, and an incorrect guess silently changes launches. Omitting it means a blank selection passes no flag and every explicitly picked level does.",
    "args": ["--effort", "{{reasoning}}"],
    "levels": [
      { "id": "low", "label": "Low" },
      { "id": "medium", "label": "Medium" },
      { "id": "high", "label": "High" },
      { "id": "xhigh", "label": "Extra high" },
      { "id": "max", "label": "Max" }
    ]
  },
  "themeSelection": {
    "args": ["--settings", "{\"theme\":\"{{colorScheme}}\"}"]
  },
  "launchSettings": {
    "$comment": "claude --settings '<json>', verified against the installed CLI 2.1.268 on 2026-09-11 and documented as applying above the user, project and local settings files and below managed settings, writing nothing to disk. It carries what a plugin cannot: Claude Code's plugin settings.json accepts only `agent` and `subagentStatusLine`, so the status line the app reads context usage, cost and lines-changed from must arrive here. The theme rides the same document rather than a second --settings, whose repeatability is undocumented.",
    "args": ["--settings", "{{launchSettingsJson}}"]
  },
  "launchPlugins": {
    "$comment": "claude --plugin-dir <dir>, verified against the installed CLI 2.1.268 on 2026-09-11: a directory passed this way loads its skills, its hooks/hooks.json and its .mcp.json for THAT SESSION ONLY — `claude mcp list` shows the server as plugin:<name>:<server> with the flag and not without it, and nothing is written to ~/.claude or to the project. The flag is repeatable and must name each plugin directory: pointing it at the marketplace root (the directory holding .claude-plugin/marketplace.json) loaded neither child plugin. Hook commands in a plugin may use ${CLAUDE_PLUGIN_ROOT}, and the launch env reaches the hook process, which is how the reporter finds its socket.",
    "args": ["--plugin-dir", "{{pluginDir}}"]
  },
  "launch": {
    "argv": [
      "{{binary}}",
      { "spreadIf": "permissionArgs" },
      { "spreadIf": "modelArgs" },
      { "spreadIf": "reasoningArgs" },
      { "spreadIf": "themeArgs" },
      { "spreadIf": "contextArgs" },
      { "spreadIf": "pluginDirArgs" },
      { "spreadIf": "launchSettingsArgs" },
      "--session-id",
      "{{sessionId}}",
      { "valueIf": "prompt", "value": "{{prompt}}" }
    ]
  },
  "resume": {
    "supported": true,
    "argv": [
      "{{binary}}",
      { "spreadIf": "permissionArgs" },
      { "spreadIf": "themeArgs" },
      { "spreadIf": "contextArgs" },
      { "spreadIf": "pluginDirArgs" },
      { "spreadIf": "launchSettingsArgs" },
      "--resume",
      "{{sessionId}}",
      { "valueIf": "prompt", "value": "{{prompt}}" }
    ]
  },
  "promptInjection": {
    "mode": "positional-arg"
  },
  "contextInjection": {
    "$comment": "claude --append-system-prompt-file <path>, verified against the installed CLI 2.1.263: the options list documents --append-system-prompt, and --bare's own text names the file variant as --append-system-prompt[-file]. A FILE rather than the text because the host-context document is multi-line markdown, and argv is not where a document belongs. Spread into launch AND resume argv, so a resumed session is re-told what the host said.",
    "mode": "argv",
    "args": ["--append-system-prompt-file", "{{contextFile}}"]
  },
  "completion": {
    "mode": "process-exit"
  },
  "mcpConfig": {
    "path": "{{workspaceRoot}}/.mcp.json",
    "format": "claude-code"
  },
  "skillIntegration": {
    "support": "native",
    "harnessId": "claude",
    "installTargets": [
      {
        "scope": "workspace",
        "path": "{{workspaceRoot}}/.claude/skills/{{skillId}}",
        "format": "claude-code",
        "$comment": "restartRequired verified false on 2026-07-29: a skill directory created while `claude` was already running appeared in its /skill picker without a restart. kimi-claude and zai run the same `claude` binary against the same .claude/skills, so the finding covers all three.",
        "restartRequired": false
      }
    ],
    "invocation": {
      "fileDropTemplate": "/{{skillId}} {{path}}",
      "explicitTemplate": "/{{skillId}}",
      "nativeSlashCommand": true,
      "mentionPrefix": "/"
    }
  },
  "agentStateSpec": {
    "$comment": "Authoritative agent-state via Claude Code lifecycle hooks, merged as tagged entries into the workspace settings. PreToolUse is register:false deliberately: PreToolUse and PostToolUse fire once per tool call each, and PostToolUse alone is load-bearing (its thinking frame is the only signal that clears awaiting_input after a permission is answered), so registering only PostToolUse halves the per-tool reporter spawns. The Notification allow-list keeps informational types (idle_prompt, auth_success, elicitation_complete/response, agent_completed — documented set as of 2026-07-08) from parking a session as falsely needs-input forever (the 2026-07-07 incident); if a future Claude build adds a new BLOCKING notification_type, extend oneOf here. FileChanged (Claude Code 2.1.266) is registered for the agent CHANGELIST, not the phase: its payload is file_path + event (change|add|unlink) with no diff, so the reporter forwards it as a path-only touch and an edit made through Bash, a formatter or codegen still reaches the ledger at file level. It fires ONLY for paths a hook has armed with hookSpecificOutput.watchPaths — nothing arms any today, so it is declared register:false: the reporter already reads the event, and the day something arms watch paths the registration is one flag away. Two reasons it stays unwritten until that arming is decided: arming a whole repository would spawn a reporter per file change (a git checkout is thousands), and the declared phase is `thinking` only because the event table requires one — a watcher event firing while the session was idle would move the phase, which is the wrong kind of wrong.",
    "registration": { "kind": "settings-json", "path": ".claude/settings.local.json" },
    "statusLine": true,
    "events": [
      { "event": "SessionStart", "phase": "starting" },
      { "event": "UserPromptSubmit", "phase": "thinking" },
      { "event": "PreToolUse", "phase": "tool_use", "register": false },
      { "event": "PostToolUse", "matcher": "*", "phase": "thinking" },
      { "event": "FileChanged", "phase": "thinking", "register": false },
      {
        "event": "Notification",
        "phase": "awaiting_input",
        "when": { "field": "notificationType", "oneOf": ["permission_prompt", "elicitation_dialog", "agent_needs_input"] }
      },
      { "event": "Stop", "phase": "idle", "turnEnd": true },
      { "event": "SubagentStart", "phase": "tool_use", "background": "start" },
      { "event": "SubagentStop", "phase": "thinking", "background": "stop" },
      { "event": "SessionEnd", "phase": "exited" }
    ]
  },
  "detect": {
    "versionArgs": ["--version"]
  },
  "package": {"npm": "@anthropic-ai/claude-code", "brew": "claude-code"},
  "update": {
    "args": ["update"]
  },
  "install": {
    "darwin": [
      {
        "id": "official",
        "label": "Official installer (recommended)",
        "shell": "curl -fsSL https://claude.ai/install.sh | bash",
        "recommended": true,
        "requires": "curl"
      },
      {
        "id": "npm",
        "label": "npm (requires Node.js)",
        "shell": "npm install -g @anthropic-ai/claude-code",
        "requires": "npm"
      }
    ],
    "linux": [
      {
        "id": "official",
        "label": "Official installer (recommended)",
        "shell": "curl -fsSL https://claude.ai/install.sh | bash",
        "recommended": true,
        "requires": "curl"
      },
      {
        "id": "npm",
        "label": "npm (requires Node.js)",
        "shell": "npm install -g @anthropic-ai/claude-code",
        "requires": "npm"
      }
    ],
    "wsl": [
      {
        "id": "official",
        "label": "Official installer in WSL (recommended)",
        "shell": "curl -fsSL https://claude.ai/install.sh | bash",
        "recommended": true,
        "requires": "curl"
      },
      {
        "id": "npm",
        "label": "npm in WSL (requires Node.js)",
        "shell": "npm install -g @anthropic-ai/claude-code",
        "requires": "npm"
      }
    ],
    "win32": [
      {
        "id": "official",
        "label": "Official installer (recommended)",
        "shell": "irm https://claude.ai/install.ps1 | iex",
        "recommended": true
      },
      {
        "id": "npm",
        "label": "npm (requires Node.js)",
        "shell": "npm install -g @anthropic-ai/claude-code",
        "requires": "npm"
      }
    ]
  },
  "capabilities": {
    "resumeSession": true,
    "sessionIdFromCaller": true,
    "toolUse": true,
    "mcpServers": true
  }
}
