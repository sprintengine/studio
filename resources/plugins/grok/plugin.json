{
  "id": "grok",
  "displayName": "Grok Build",
  "publisher": "xai",
  "version": 1,
  "summary": "xAI's Grok Build coding agent, driven in a terminal.",
  "category": "Agent CLI",
  "icon": "icon.svg",
  "binary": "grok",
  "permissionPresets": {
    "manual": {
      "label": "Manual — ask before every action",
      "args": ["--permission-mode", "default"]
    },
    "auto": {
      "$comment": "Grok ships the full Claude Code mode set: `grok --help` lists `--permission-mode [default, acceptEdits, auto, dontAsk, bypassPermissions, plan]`. It also has `--sandbox <PROFILE>`, not used here. --trust rides this rung as well as bypass, decided deliberately (MC-2211): without it Grok skips project-level .grok hooks, so no agent-state frames arrive and the session converts to `stalled` via the runtime watchdog — an auto session that always reads as stalled is worse than useless. The trust decision is defensible HERE because auto is not the safe default: `manual` is, and it carries no trust, so a freshly cloned untrusted repo still gets no repo-committed hooks unless the user asks for autonomy.",
      "label": "Auto — Grok reviews each action instead of you",
      "args": ["--permission-mode", "auto", "--trust"]
    },
    "bypass": {
      "$comment": "--trust rides this preset, never the default launch argv: it does not just enable the studio's own .grok/hooks agent-state reporter — it trusts EVERY project-level .grok config, including hooks committed in the repo itself, so a freshly cloned untrusted repo must not get it under the safe default. Without trust Grok skips project hooks, so no frames arrive and the session converts to the stalled phase via the runtime watchdog (there is no output-timing fallback any more); the user can grant trust in the TUI via /hooks-trust. Kept as `--always-approve` rather than switched to `--permission-mode bypassPermissions`: both exist in `grok --help` and this one is what shipped and is covered by tests, so MC-2211 adds the missing auto rung without churning a working bypass.",
      "label": "Bypass all approvals (dangerous)",
      "args": ["--always-approve", "--trust"]
    }
  },
  "modelSelection": {
    "args": ["--model", "{{model}}"],
    "options": [
      { "id": "grok-4.5", "label": "Grok 4.5" },
      { "id": "grok-build-0.1", "label": "Grok Build 0.1" }
    ],
    "allowCustomId": true
  },
  "launch": {
    "argv": [
      "{{binary}}",
      { "spreadIf": "permissionArgs" },
      { "spreadIf": "modelArgs" },
      { "spreadIf": "contextArgs" },
      "--session-id",
      "{{sessionId}}"
    ]
  },
  "resume": {
    "supported": true,
    "argv": [
      "{{binary}}",
      { "spreadIf": "permissionArgs" },
      { "spreadIf": "modelArgs" },
      { "spreadIf": "contextArgs" },
      "--resume",
      "{{sessionId}}"
    ]
  },
  "promptInjection": {
    "$comment": "Grok Build's interactive TUI documents no positional prompt argument (headless -p is a one-shot, not a session). The pattern (?!) never matches by design: Grok's real input-ready marker is unknown, and any guessable pattern (e.g. the brand name) matches the startup banner before the TUI accepts input — injecting there both swallows the keystrokes and cancels the timeout fallback, while a post-timeout late match would inject a second copy. Never-match makes delivery deterministic: exactly one injection, via the runtime's timeout fallback, after the TUI has had timeoutMs to become ready. If a real install confirms `grok \"prompt\"` works interactively (Claude Code parity), flip mode to positional-arg — config-only change.",
    "mode": "send-after-ready",
    "readiness": {
      "type": "output-match",
      "pattern": "(?!)",
      "timeoutMs": 8000
    }
  },
  "contextInjection": {
    "$comment": "grok --append-system-prompt <text>, verified accepted by the installed Grok Build 1.0.13 as an undocumented compat alias of the documented --rules (\"Extra rules to append to the system prompt\"). Text rather than a file: grok documents no file-taking variant. Spread into launch AND resume argv, so a resumed session is re-told.",
    "mode": "argv",
    "args": ["--append-system-prompt", "{{contextText}}"]
  },
  "completion": {
    "mode": "process-exit"
  },
  "mcpConfig": {
    "path": "{{workspaceRoot}}/.mcp.json",
    "format": "claude-code"
  },
  "skillIntegration": {
    "support": "native",
    "harnessId": "grok",
    "installTargets": [
      {
        "scope": "workspace",
        "path": "{{workspaceRoot}}/.grok/skills/{{skillId}}",
        "format": "claude-code",
        "$comment": "restartRequired NOT verified on 2026-07-29: the probe could not reach an interactive session because this machine is not signed in to Grok (the TUI sits on its browser-approval screen). Left true — a banner that over-warns is recoverable, one that under-warns is not. Verify by adding a skill directory while `grok` runs and checking its /skill picker.",
        "restartRequired": true
      }
    ],
    "invocation": {
      "fileDropTemplate": "/{{skillId}} {{path}}",
      "explicitTemplate": "/{{skillId}}",
      "nativeSlashCommand": true,
      "mentionPrefix": "/"
    }
  },
  "agentStateSpec": {
    "$comment": "Grok Build ships Claude-style lifecycle hooks. Discovery is per-file: project hooks are standalone JSON files under .grok/hooks/, so we own our config file outright (owned-json). Project hooks only run once the folder is trusted — see the bypass_all $comment. VERIFIED 2026-09-09 against grok 1.0.13 (xAI's Grok Build TUI at /opt/homebrew/bin/grok — NOT the community `grok-cli`, whose hook engine fires only for its bash tool), by reading the hook reference embedded in the binary and by writing candidate .grok/hooks/*.json files and counting what `grok inspect` loaded. CONFIG spelling: the event names below are written PascalCase and grok loads them (15/15 in a probe file). It also accepts snake_case and Cursor camelCase, but NOT kebab-case or SCREAMING, and it silently skips names it does not know — so this is a fixed alias set, not case-insensitivity, and our PascalCase is inside it. PAYLOAD spelling differs from the config spelling: the documented stdin payload stamps \"hookEventName\": \"pre_tool_use\" (snake_case), while field NAMES are camelCase (hookEventName / sessionId / toolName / toolInput, each with a snake_case serde alias the shared reporter also reads). That mismatch was MC-2520: the main process compared this manifest's PascalCase to the frame's snake_case EXACTLY and dropped every Grok frame, so a Grok agent never left `starting` until the stall watch flagged it. Fixed in resolveAgentStateEvent by canonical (case- and _/- insensitive) comparison, so the table below keeps grok's own written form and the registration emitter still writes it verbatim. Grok's documented Notification types are `idle_prompt`, `permission_prompt`, `task_complete`: `permission_prompt` below is CONFIRMED real, `elicitation_dialog` and `agent_needs_input` are harmless carry-over grok never sends, and `idle_prompt` stays off the list deliberately (a false needs-input parks a session forever; a false idle is recoverable). NOT verified live: no payload was captured from a running session — this machine is not signed in to Grok (`grok -p` exits \"Not signed in\"), so whether a file edit fires PreToolUse/PostToolUse, and the edit tool's name in a real payload, still rest on the embedded docs (which name grok's edit tool `search_replace`, aliased from Edit/Write/MultiEdit; Bash is `run_terminal_command`). Grok also ships events this table does not use yet: PostToolUseFailure, PermissionDenied, StopFailure, StopCancelled, SubagentStart, PreCompact, PostCompact.",
    "registration": { "kind": "owned-json", "path": ".grok/hooks/multicode-agent-state.json" },
    "events": [
      { "event": "SessionStart", "phase": "starting" },
      { "event": "UserPromptSubmit", "phase": "thinking" },
      { "event": "PreToolUse", "phase": "tool_use", "register": false },
      { "event": "PostToolUse", "matcher": "*", "phase": "thinking" },
      {
        "event": "Notification",
        "phase": "awaiting_input",
        "when": { "field": "notificationType", "oneOf": ["permission_prompt", "elicitation_dialog", "agent_needs_input"] }
      },
      { "event": "Stop", "phase": "idle", "turnEnd": true },
      { "event": "SubagentStop", "phase": "thinking" },
      { "event": "SessionEnd", "phase": "exited" }
    ]
  },
  "detect": {
    "versionArgs": ["--version"]
  },
  "package": {"npm": "@xai-official/grok"},
  "install": {
    "darwin": [
      {
        "id": "official",
        "label": "Official installer (recommended)",
        "shell": "curl -fsSL https://x.ai/cli/install.sh | bash",
        "recommended": true,
        "requires": "curl"
      },
      {
        "id": "npm",
        "label": "npm (requires Node.js)",
        "shell": "npm install -g @xai-official/grok",
        "requires": "npm"
      }
    ],
    "linux": [
      {
        "id": "official",
        "label": "Official installer (recommended)",
        "shell": "curl -fsSL https://x.ai/cli/install.sh | bash",
        "recommended": true,
        "requires": "curl"
      },
      {
        "id": "npm",
        "label": "npm (requires Node.js)",
        "shell": "npm install -g @xai-official/grok",
        "requires": "npm"
      }
    ],
    "wsl": [
      {
        "id": "official",
        "label": "Official installer in WSL (recommended)",
        "shell": "curl -fsSL https://x.ai/cli/install.sh | bash",
        "recommended": true,
        "requires": "curl"
      },
      {
        "id": "npm",
        "label": "npm in WSL (requires Node.js)",
        "shell": "npm install -g @xai-official/grok",
        "requires": "npm"
      }
    ],
    "win32": [
      {
        "id": "official",
        "label": "Official installer (recommended)",
        "shell": "irm https://x.ai/cli/install.ps1 | iex",
        "recommended": true
      },
      {
        "id": "npm",
        "label": "npm (requires Node.js)",
        "shell": "npm install -g @xai-official/grok",
        "requires": "npm"
      }
    ]
  },
  "capabilities": {
    "$comment": "resumeSession is intentionally false pending end-to-end verification (the MC-1464/MC-1465 discipline: an unverified resume path must not risk a lost conversation). Grok Build documents claude-style resume (-s/--session-id at launch, -r/--resume <id> on relaunch) and the resume argv above is wired for it, so once resume is verified against a real Grok Build install, flip this to true — no code change needed. sessionIdFromCaller is true because launch passes our minted --session-id.",
    "resumeSession": false,
    "sessionIdFromCaller": true,
    "toolUse": true,
    "mcpServers": true
  }
}
